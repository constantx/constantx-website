---
type Props = {
  class?: string;
};

const { class: className = '' } = Astro.props;
---

<div class:list={[
  'flex-1 grid grid-cols-6 text-cream relative',
  className
]} style="gap: 1px;">
  <slot />
</div> 